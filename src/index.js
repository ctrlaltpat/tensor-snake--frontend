document.addEventListener('DOMContentLoaded', () => {
  // get data - scores, users, reviews
  // construct/update home page - options ( play, view scores)
  // if play - render form for player name, avatar, controls(show default)---and/or user selection 
  // if view scores - render scoreboard

  
  console.log('index')
})

const amazeballs = new Game();
